{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "imports": {
    "#root/*": "./*",
    "#packages/*": "./packages/*",
    "#common/*": "./packages/common/*",
    "#type-graphql/*": "./packages/server/api/type-graphql"
  },
  "scripts": {
    "dev": "yarn start:web && yarn dev:server",
    "start:web": "vite packages/vueApp",
    "preview": "vite serve packages/vueApp",
    "dev:server": "nodemon packages/server/dist/src/index.js",
    "generate": "cd packages/server && npx prisma generate",
    "start:server": "node --experimental-specifier-resolution=node packages/server/dist/src/index.js",
    "watch:server": "cd packages/server && yarn watch",
    "compile": "cd packages/server && yarn build",
    "build": "yarn build:server && yarn build:vueApp && yarn build:common",
    "build:common": "cat 'Building common!",
    "build:vueApp": "vite build packages/vueApp"
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "jest": "^27.0.6",
    "lerna": "^4.0.0",
    "typescript": "^4.3.5"
  }
}