{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
],
  "scripts": {
    "start:web": "vite packages/vueApp",
    "preview": "vite serve packages/vueApp",
    "start:server": "cat 'server started!",
    "build": "yarn build:packages && yarn build:vueApp",
    "build:packages": "lerna run build --scope '@polomaapp/*'",
    "build:vueApp": "lerna run build --scope 'vueApp'"
},
  "devDependencies": {
    "eslint": "^7.32.0",
    "jest": "^27.0.6",
    "lerna": "^4.0.0",
    "typescript": "^4.3.5"
  }
}
